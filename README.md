### Installation Guide
1. Install npm packages
   
    `npm install` or `yarn`

2. Run the project

    `npm run dev` or `yarn start`

3. Build the project

    `npm run build` or `yarn build`

### Deployment

